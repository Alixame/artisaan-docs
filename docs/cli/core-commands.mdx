---
title: "CLI Core - Main Commands and Primary Workflow"
description: "Practical guide to the main Artisaan CLI commands and the recommended documentation generation workflow."
summary: "Learn how to use Artisaan CLI core commands to initialize a project, generate docs locally, and sync to a remote environment."
keywords:
  - artisaan cli
  - core commands
  - init
  - generate
  - docs sync
  - documentation
tags:
  - cli
  - core
  - docs
type: "public"
complexity: "low"
section: "CLI"
sidebarOrder: 2
sidebarLabel: "Core Commands"
route: "cli/core-commands"
lastUpdated: 2026-03-02
---

<DocHeader
  title="Core Commands"
  description="Main workflow to generate and publish documentation with Artisaan CLI"
/>

## Main workflow

<Steps>
  <Step
    title="Initialize the project"
    variant="active"
    code="artisaan init"
  >
    Creates the `.artisaan/` folder and the base project configuration file.
  </Step>

  <Step
    title="Review configuration"
    code=".artisaan/config.json"
  >
    Confirm language, project type, and search paths before generation.
  </Step>

  <Step
    title="Generate documentation"
    code="artisaan docs generate"
  >
    Processes your code and generates/updates documentation files.
  </Step>

  <Step
    title="Sync to remote"
    code="artisaan docs sync"
  >
    Sends docs to the configured bucket/project, updating published pages.
  </Step>
</Steps>

## Main commands

| Command | Purpose | When to use |
| --- | --- | --- |
| `artisaan init` | Initializes local configuration | First run in the repository |
| `artisaan docs generate` | Generates docs from your code | After project changes |
| `artisaan docs sync` | Publishes/syncs remote docs | After local validation |

## Run by operating system

<CodeTabs>
  <CodeTabItem label="MacOS/Linux">
artisaan init
artisaan docs generate
artisaan docs sync
  </CodeTabItem>

  <CodeTabItem label="Windows">
artisaan init
artisaan docs generate
artisaan docs sync
  </CodeTabItem>
</CodeTabs>

## Minimum recommended configuration

<CodeBlock>
```json
{
  "slug": "my-project",
  "language": "typescript",
  "project": "nextjs",
  "search_paths": [
    "app",
    "lib",
    "src"
  ],
  "ignore_paths": [
    "node_modules",
    "dist",
    ".next"
  ]
}
```
</CodeBlock>

## Common issues

### `missing field "slug"`

This error occurs in `artisaan docs sync` when `.artisaan/config.json` does not contain the `slug` field.

<CodeBlock>
Add "slug" to the .artisaan/config.json file.
Example: "slug": "my-project"
</CodeBlock>

### Empty or incomplete documentation

- Check whether your source folders are listed in `search_paths`.
- Confirm that relevant content is not blocked in `ignore_paths`.
- Run `artisaan docs generate` again after adjustments.

## Quick checklist

- `artisaan init` executed in the correct repository.
- `.artisaan/config.json` includes `slug`, `language`, and `project`.
- `search_paths` covers the real source code folders.
- `artisaan docs generate` completes without errors before `artisaan docs sync`.
